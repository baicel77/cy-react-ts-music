const theme = {
  primary: '#c20c0c',
  mixin: {
    // boxShadow: `
    //   transition: box-shadow .2s;
    //   &:hover {
    //     box-shadow: 0 2px 4px rgba(0, 0, 0, .18);
    //   }
    // `
  }
}

export default theme
